# Clearing 프로젝트 주요 수정 사항 상세 내역 (Cumulative Log)

이 문서는 시스템의 모든 주요 변경 사항, 버그 수정, 기능 고도화 내역을 날짜와 시간 순으로 누적하여 기록합니다.

---

### **[2026-04-25 05:26] GitHub 자동화 도구 개선 (v2)**
*   **호환성 및 안정성 강화**: 윈도우 날짜 형식에 따른 오류를 방지하기 위해 PowerShell 기반 날짜 추출 로직을 적용함.
*   **예외 처리 추가**: 변경 사항이 없을 때의 커밋 오류를 방지하고, 현재 브랜치를 자동으로 탐색하여 푸시하도록 개선함.

---

### **[2026-04-23 21:55] 시스템 고도화 및 안정화**
*   **갱폼 인양-청소 연동 알림**: 대시보드에 인양 완료 층 대비 2층 아래 청소 대상을 자동으로 추출하여 표시하는 카드 추가.
*   **배치도 시각화 강화**: `ElevationView` 층수 레이블 옆에 🏗️(갱폼) 아이콘 표시 및 청소 권장 층 하이라이트 적용.
*   **보안 헤더 전역 적용**: `Worker`, `Cost`, `Personnel` 등 모든 관리 컴포넌트 API 요청 시 `X-Site-Id` 및 `Authorization` 헤더 강제 적용.
*   **단가 관리 모달 수정**: `z-index` 상향 및 에러 핸들링 추가로 단가 버튼 무반응 오류 해결.

### **[2026-04-23 21:50] 단가 관리 기능 정상화**
*   **단가 조회 쿼리 개선**: 특정 월 조회 시 이전 달의 유효 단가를 자동으로 추적하여 계승하도록 백엔드 로직 수정.
*   **DB 제약 조건 마이그레이션**: `worker_wage_history` 테이블의 UNIQUE 제약 조건에 `site_id`를 추가하여 현장별 데이터 독립성 보장.

### **[2026-04-22 23:52] 오류 수정 및 지침 강화**
*   **WorkerManager 백화 현상 해결**: 누락되었던 `dayjs` 임포트 추가를 통해 작업자 탭 백화 현상 해결 [2026-04-22 23:52]
*   **지침 업데이트 (`gemini.md`)**: '승인 후 구현' 원칙 및 모든 마크다운 파일 타임스탬프 의무화 적용.

### **[2026-04-22 23:35] 배치도 시각적 최적화**
*   **세대별 중복 횟수 뱃지**: 기록이 여러 개인 세대에 중복 횟수를 숫자로 표시.
*   **색상 체계 변경**: 작업 진행 중 상태를 시인성이 높은 'Sky Blue'로 변경하고 깜빡임 효과 제거.

### **[2026-04-22 23:14] DB 스키마 및 현장 관리 보정**
*   **현장(sites) 테이블 확장**: 주소, 시작일, 종료일 컬럼 추가 및 마이그레이션.
*   **현장 선택 인터페이스**: 로그인 직후 현장을 선택할 수 있는 `SiteSelector` 구현.

---
*본 문서는 모든 주요 수정 사항 발생 시 실시간으로 업데이트됩니다.*
